# Deployment Configuration for Rando Project
# Copy this file to deploy.config.local and update the values

# Server connection details
SERVER_USER=your_username
SERVER_HOST=your_server_ip_or_hostname
SERVER_PATH=/path/to/your/server/directory

# Git branch configuration
LOCAL_BRANCH=main
REMOTE_BRANCH=main

# Optional: SSH key path (if not using default)
# SSH_KEY_PATH=~/.ssh/id_rsa

# Optional: Custom SSH options
# SSH_OPTIONS=-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null

# Optional: Pre-deployment commands (run on server before update)
# PRE_DEPLOY_CMD="echo 'Stopping services...' && sudo systemctl stop conan-exiles"

# Optional: Post-deployment commands (run on server after update)
# POST_DEPLOY_CMD="echo 'Starting services...' && sudo systemctl start conan-exiles"
