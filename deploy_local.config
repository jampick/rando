# Local Deployment Configuration for Rando Project
# Copy this file to deploy_local.config.local and update the values

# GitHub repository details
GITHUB_REPO=https://github.com/jampick/rando.git

# Git branch configuration
LOCAL_BRANCH=main
REMOTE_BRANCH=main

# Server paths
DEPLOY_PATH=C:\MoonTideTools
BACKUP_PATH=C:\MoonTideTools\backup

# Service names (adjust these for your specific Windows services)
# These are the names that appear in Windows Services (services.msc)
CONAN_SERVICES=Conan Exiles,Conan Exiles Dedicated Server

# Optional: Custom Git options
# GIT_OPTIONS=--depth 1

# Optional: Pre-deployment commands (run before update)
# PRE_DEPLOY_CMD=echo "Stopping services..." && net stop "Conan Exiles"

# Optional: Post-deployment commands (run after update)
# POST_DEPLOY_CMD=echo "Starting services..." && net start "Conan Exiles"

# Optional: Backup settings
# BACKUP_ENABLED=true
# BACKUP_RETENTION_DAYS=7
# BACKUP_EXCLUDE=logs,temp,*.tmp

# Optional: Notification settings
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your_webhook_here
# NOTIFY_ON_SUCCESS=true
# NOTIFY_ON_FAILURE=true
